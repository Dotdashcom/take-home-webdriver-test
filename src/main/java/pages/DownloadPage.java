package pages;

import core.AbstractWebPage;
import org.openqa.selenium.WebDriver;

public class DownloadPage extends AbstractWebPage {

    public DownloadPage(WebDriver driver) {
        super(driver);
    }
}

