package pages;

import core.AbstractWebPage;
import org.openqa.selenium.WebDriver;

public class FloatingMenuPage extends AbstractWebPage {

    public FloatingMenuPage(WebDriver driver) {
        super(driver);
    }
}
