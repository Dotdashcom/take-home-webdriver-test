package com.assignments.enums;

/**
 * Enums to restrict the users to choose an appropriate waiting strategy before operating an
 * element.
 */
public enum WaitStrategy {
  VISIBLE,
  CLICKABLE,
  PRESENCE,
  NONE
}
