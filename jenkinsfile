pipeline {
  agent any
  
  stages {
    stage('Build') {
      steps {
        sh 'apt-get update && apt-get install -y maven'
        sh 'mvn clean package'
      }
    }
    stage('Test') {
      steps {
        sh 'mvn test'
      }
    }
  }
}
